namespace Meerkat.Models
{
    /// <summary>
    /// The states that the state machine can be changed to for our state tracker.
    /// </summary>
    public enum State
    {
        INSERT,
        NAVIGATION
    }
}
